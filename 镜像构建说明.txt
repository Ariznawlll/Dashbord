================================================================================
                    Docker 镜像构建与部署 - 快速参考
================================================================================

📦 三种构建方式：

方式一：自动化脚本（最简单）
---------------------------------
./build-images.sh v1.0.0

方式二：Makefile（推荐）
---------------------------------
make build VERSION=v1.0.0
make push REGISTRY=harbor.company.com/project

方式三：手动构建
---------------------------------
docker build -t dashboard-backend:v1.0.0 -f backend/Dockerfile backend/
docker build -t dashboard-frontend:v1.0.0 -f frontend/Dockerfile frontend/

================================================================================

🚀 部署流程：

1. 开发机器上构建镜像
   ./build-images.sh v1.0.0

2. 推送到镜像仓库
   docker push your-registry.com/dashboard-backend:v1.0.0
   docker push your-registry.com/dashboard-frontend:v1.0.0

3. 服务器上拉取并部署
   cd /opt/dashboard
   docker-compose pull
   docker-compose up -d

================================================================================

📁 相关文档：

- DOCKER_DEPLOYMENT.md  - 详细的 Docker 部署文档
- 快速部署指南.md      - 图文并茂的快速指南
- DEPLOYMENT.md         - 通用部署文档
- 部署说明.md          - 中文部署说明

================================================================================

🔧 常用命令：

# 使用 Makefile
make build              # 构建镜像
make push               # 推送镜像
make deploy             # 部署服务
make logs               # 查看日志
make help               # 查看所有命令

# 使用 Docker Compose
docker-compose up -d    # 启动服务
docker-compose ps       # 查看状态
docker-compose logs -f  # 查看日志
docker-compose down     # 停止服务

# 使用脚本
./build-images.sh v1.0.0    # 构建镜像
./deploy.sh start           # 启动服务
./deploy.sh logs            # 查看日志

================================================================================

💡 提示：

1. 首次使用前，修改 build-images.sh 中的 REGISTRY 地址
2. 部署前，确保配置好 .env 文件
3. 生产环境建议使用具体版本号，不要用 latest
4. 定期备份 .env 配置文件

================================================================================

❓ 需要帮助？

查看详细文档：
- cat DOCKER_DEPLOYMENT.md
- cat 快速部署指南.md

或运行：
- make help
- ./build-images.sh --help
- ./deploy.sh help

================================================================================
